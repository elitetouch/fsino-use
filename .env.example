# Backend API base URL — the tenant (user-app) API surface.
NEXT_PUBLIC_API_BASE_URL=https://api.fsinnovation.net/api/v1

# Optional: app name shown in tab title + footer.
NEXT_PUBLIC_APP_NAME=Farm Support Innovation
